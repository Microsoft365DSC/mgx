<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Mgx.Cmdlets</RootNamespace>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
    <!-- net10.0 carries System.IO.Pipelines in Microsoft.NETCore.App, so the SDK prunes the
         package reference below and nothing reaches the build output. The reference exists to
         put that file in Dependencies/ for a host that does not supply it, so pruning has to
         stay off here. Upstream targets net8.0, where the package is not pruned. -->
    <RestoreEnablePackagePruning>false</RestoreEnablePackagePruning>
    <!-- See Mgx.Engine.csproj: CA1416 keeps Windows-only directory APIs out of the
         cross-platform code paths at build time rather than at a customer's runtime. -->
    <WarningsAsErrors>$(WarningsAsErrors);CA1416</WarningsAsErrors>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Mgx.IntegrationTests" />
    <InternalsVisibleTo Include="Mgx.E2ETests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="System.Management.Automation" Version="7.4.7" PrivateAssets="all" />
    <!-- Carried, not compiled against. Nothing here or in the engine uses System.IO.Pipelines;
         it is in the closure of the System.Text.Json the HOST supplies. The engine's reference
         is to System.Text.Json 8.0, which needs no pipelines, but a host binds that reference to
         whatever version it has: PowerShell 7.5 to 9.0 and 7.6 to 10.0, and both of those take
         PipeReader and PipeWriter across their surface. net8.0 - what src/ targets - does not
         carry System.IO.Pipelines in Microsoft.NETCore.App, so where the host does not supply it
         either the first call that touches the JSON stack throws FileNotFoundException: the
         resolver has only Dependencies/ to offer and nothing put it there. This reference is what
         puts it there, and one 10.0.x file answers both requests - a Resolving handler's answer
         is taken as given. ModuleAssemblyClosureTests pins the whole class of gap. -->
    <PackageReference Include="System.IO.Pipelines" Version="10.0.11" GeneratePathProperty="true" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Mgx.Engine\Mgx.Engine.csproj" />
  </ItemGroup>

  <!-- Post-build: auto-stage DLLs into module/ so IDE builds are never stale.

       Release only, deliberately. This target used to run for every configuration, which
       meant `dotnet test` - Debug by default - silently replaced a Release-staged module/
       with Debug binaries. The natural release sequence is build.ps1, then tests, then
       Publish-Module, so the test step undid the build step and the Gallery would have
       received an unoptimised build with no warning anywhere. CI never hit it (it builds
       Release and tests without rebuilding); publishing from a developer machine did.

       A Debug build now leaves module/ alone. To stage Debug on purpose, ask for it:
       ./build.ps1 -Configuration Debug, which copies explicitly and does not rely on this. -->
  <!-- Pruning is off for this package, but the file still has to be placed beside the build
       output before anything copies it onward. -->
  <Target Name="CopyPipelinesDependency" AfterTargets="Build">
    <Copy SourceFiles="$(PkgSystem_IO_Pipelines)\lib\net10.0\System.IO.Pipelines.dll" DestinationFolder="$(TargetDir)" SkipUnchangedFiles="true" />
  </Target>

  <Target Name="StageModuleOutput" AfterTargets="Build" DependsOnTargets="CopyPipelinesDependency" Condition="'$(Configuration)' == 'Release'">
    <PropertyGroup>
      <ModuleRoot Condition="'$(ModuleRoot)' == ''">$(MSBuildThisFileDirectory)../../module</ModuleRoot>
      <DepsDir>$(ModuleRoot)/Dependencies</DepsDir>
    </PropertyGroup>

    <MakeDir Directories="$(DepsDir)" />

    <!-- Cmdlets + Engine to module root -->
    <Copy SourceFiles="$(TargetDir)Mgx.Cmdlets.dll" DestinationFolder="$(ModuleRoot)" SkipUnchangedFiles="true" />
    <Copy SourceFiles="$(TargetDir)Mgx.Engine.dll" DestinationFolder="$(ModuleRoot)" SkipUnchangedFiles="true" />
    <Copy SourceFiles="$(TargetDir)Mgx.Cmdlets.pdb" DestinationFolder="$(ModuleRoot)" SkipUnchangedFiles="true" Condition="Exists('$(TargetDir)Mgx.Cmdlets.pdb')" />
    <Copy SourceFiles="$(TargetDir)Mgx.Engine.pdb" DestinationFolder="$(ModuleRoot)" SkipUnchangedFiles="true" Condition="Exists('$(TargetDir)Mgx.Engine.pdb')" />
    <Copy SourceFiles="$(TargetDir)Mgx.Cmdlets.deps.json" DestinationFolder="$(ModuleRoot)" SkipUnchangedFiles="true" Condition="Exists('$(TargetDir)Mgx.Cmdlets.deps.json')" />

    <!-- ALC-isolated dependencies, and the one assembly carried for a host that may lack it -->
    <Copy SourceFiles="$(TargetDir)Polly.Core.dll" DestinationFolder="$(DepsDir)" SkipUnchangedFiles="true" Condition="Exists('$(TargetDir)Polly.Core.dll')" />
    <Copy SourceFiles="$(TargetDir)System.Threading.RateLimiting.dll" DestinationFolder="$(DepsDir)" SkipUnchangedFiles="true" Condition="Exists('$(TargetDir)System.Threading.RateLimiting.dll')" />
    <Copy SourceFiles="$(TargetDir)System.IO.Pipelines.dll" DestinationFolder="$(DepsDir)" SkipUnchangedFiles="true" Condition="Exists('$(TargetDir)System.IO.Pipelines.dll')" />
  </Target>

</Project>
